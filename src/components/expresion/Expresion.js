import React from "react";
import "./expresion.scss";

const Expresion = ({children}) => {

  return (
    <span className="expresion">
      {children}
    </span>
  );
};

export default Expresion;
